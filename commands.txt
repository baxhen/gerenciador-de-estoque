# create a cluster
az aks create --resource-group basicArchJs --name basicArchJsCluster --node-count 1 --generate-ssh-keys --attach-acr baxhenBasicArchJs
# use remote cluster as context
az aks get-credentials --resource-group basicArchJs --name basicArchJsCluster