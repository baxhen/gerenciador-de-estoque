version: '3'
services:
  api:
    # depends_on:
    #   - postgres
    build:
      dockerfile: Dockerfile.dev
      context: ./server
    volumes:
      - /app/node_modules
      - ./server:/app
    environment:
      - WEB_URL=https://5032be91e1e0.ngrok.io
      - MONGO_URI=mongodb+srv://admin:u901eSfoSpr4YcQy@auth.xj11d.azure.mongodb.net/auth?retryWrites=true&w=majority
      - EMAIL=leo292629@gmail.com
      - EMAIL_PASSWORD=Xuan1234
      - JWT_SECRET=uyrxvzhjnkgvwquaisdozjn2qwdasxz
      - COMPANY_NAME=ABC Livros e Brinquedos
      - FACEBOOK_URL=https://pt-br.facebook.com/www.abclivrosebrinquedos.com.br/
      - CENTRAL_AJUDA_URL=https://pt-br.facebook.com/www.abclivrosebrinquedos.com.br/
  web:
    stdin_open: true
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./web
    volumes:
      - /app/node_modules
      - ./web:/app
  nginx-proxy:
    depends_on:
      - api
      - web
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
      - '3000:80'
